name: test workflow
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: gbrls/action-dropbox-download@main
        with:
          dropbox-token: ${{ secrets.DROPBOX_TOKEN }}
          source-path: '/dotfiles/zk'
          destination-path: 'downloaded'
      - name: List artifacts
        run: |
          ls -la
          find

